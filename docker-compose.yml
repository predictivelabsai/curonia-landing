services:
  web:
    build: .
    ports:
      - "${PORT:-5001}:5001"
    environment:
      - PORT=5001
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:5001/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
